#include "ECFMP/ECFMP.h"

namespace ECFMPTest {
    class FlowSdkHeaderTest : public testing::Test
    {
    };

    TEST_F(FlowSdkHeaderTest, ItLoadsHeader)
    {
        // Dummy expectation, the purpose of this test is to make sure the header compiles properly.
        EXPECT_TRUE(true);
    }
}// namespace ECFMPTest
